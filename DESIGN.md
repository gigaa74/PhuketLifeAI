# Phuket Life — контекст продукта и разработки

Последняя актуализация: 25 августа 2026 года.

## 1. Что мы строим

Phuket Life — цифровой сервис персонального сопровождения на Пхукете.
Telegram-бот является первым интерфейсом, но конечный продукт — операционная
система, которая принимает запрос человека, уточняет только необходимые
детали, находит подходящего локального партнёра и сопровождает запрос до
понятного результата.

Главный продуктовый принцип: человек обращается в одно место и не остаётся
один на один с поиском исполнителей, проверкой информации и координацией.

## 2. Текущая бизнес-модель

Ближайший приоритет — сформировать минимальную надёжную партнёрскую сеть,
проверить передачу заявок и только затем системно наращивать клиентский поток.

Первые основные категории:

1. Жильё.
2. Транспорт и трансферы.
3. Экскурсии и активности.

Минимальная готовность категории — не менее двух надёжных партнёров и
несколько успешно проверенных заявок. Большое количество непроверенных
контактов не считается партнёрской сетью.

## 3. Основной рабочий сценарий

1. Владелец пересылает основному боту сообщение из Telegram-чата либо вставляет
   текст вручную.
2. Бот определяет тип обращения: клиент, потенциальный партнёр или неясно.
3. Для клиентского запроса бот извлекает известные параметры и показывает,
   какие критичные данные отсутствуют.
4. Бот готовит два отдельных текста:
   - персональный ответ клиенту от имени Phuket Life;
   - обезличенную структурированную заявку партнёру.
5. Владелец проверяет оба текста.
6. Отправка клиенту или партнёру выполняется только после решения владельца.
7. Контакт клиента не раскрывается партнёру до согласования следующего шага.

Автоматическая рассылка и автоматическое принятие коммерческих решений на
текущем этапе запрещены.

## 4. Архитектурные компоненты

- `bot.py` — composition root основного Telegram-бота, партнёрские и
  административные обработчики.
- `client_ai.py` — клиентская история, GigaChat transport и анализ кейса.
- `client_handler.py` — клиентский диалог, routing и запуск поиска.
- `conversation_policy.py` и `conversation_prompts.py` — правила диалога.
- `case_engine.py`, `case_flow.py`, `housing_flow.py` — клиентские кейсы.
- `partner_network.py`, `partner_handoff.py`, `partner_authority.py` —
  партнёрская сеть, полномочия и передача запросов.
- `partner_referrals.py` — запросы, которые реальные партнёры передают Phuket Life.
- `partner_applications.py` и `partner_identity_relinks.py` — заявки новых
  партнёров и безопасная смена Telegram identity.
- `manual_leads.py` — ручная квалификация пересланных лидов и подготовка текстов.
- `lead_classifier.py` и `service_labels.py` — локальная классификация ручных
  лидов и русские названия категорий без отдельного Telegram-бота.
- `database.py` — единый SQLite migration runner.
- `reliability.py` — единые ограничения частоты, безопасные повторные попытки
  внешних вызовов и структурированные журналы без пользовательских данных.
- `phuketlife.db` — production SQLite; файл не коммитится и перед изменением
  требует резервной копии.

## 5. Текущее состояние

- Базовый коммит `main` перед Sprint #12: `99ff873` — Privacy & Delivery Safety.
- Код содержит последовательность migrations 1–13. Фактическую версию production
  SQLite необходимо подтверждать read-only проверкой перед каждым rollout.
- Реальные active hybrid-партнёры: Валерия, Инна и Сергей.
- Тестовые партнёры и дубль Валерии удалены безопасной maintenance-командой.
- Role-aware `/start`, partner applications, identity relink, русские карточки
  партнёров и Partner Referral Intake реализованы.
- Manual Lead Intake принимает пересланный или скопированный текст только от
  администратора и не создаёт автоматически clients, cases или partners.
- Manual Lead Intake формирует отдельный текст клиенту и отдельную обезличенную
  заявку партнёру. Изменение закоммичено, влито в `main` и опубликовано.
- Privacy & Delivery Safety обезличивает данные ручных лидов перед внешней AI,
  ограничивает их хранение 30 днями и требует явного решения владельца перед
  каждой отправкой предложения клиенту.
- Sprint #11 безопасно развёрнут в production: схема 1–12, ручные лиды
  обезличены, integrity и сохранность остальных таблиц подтверждены.
- Sprint #12 добавил reliability foundation: лимит клиентских
  сообщений, ограниченные повторы временных внешних ошибок, SQLite WAL и
  безопасные структурированные журналы.
- Монолитный клиентский блок вынесен из `bot.py` в `client_ai.py` и
  `client_handler.py`. Публичная команда запуска и имена обработчиков сохранены;
  размер entrypoint уменьшен ниже прежнего порога 100 KB.
- Отдельные Partner Scout и Client Scout выведены из эксплуатации: доступ в
  сторонние Telegram-чаты не оправдывает стоимость и зависимость от решений
  администраторов. Их Telegram-имена сохраняются за Phuket Life, но токены не
  используются приложением. Исторические migrations 007–008 и таблица
  `scout_candidates` остаются только для совместимости и сохранности данных.
- Рабочий канал поиска — владелец пересылает найденное сообщение в основной
  бот, а Manual Lead Intake квалифицирует лид и готовит два текста.

## 6. Неприкосновенные правила безопасности

1. Numeric Telegram user ID — основной identity после реального взаимодействия.
2. Username — изменяемый контакт, а не надёжный первичный ключ.
3. Не создавать fake Telegram ID.
4. Не создавать active partner без решения владельца.
5. Не менять approved commercial terms автоматически.
6. Не показывать клиенту внутреннюю комиссию, телефоны и служебные заметки.
7. Не обещать наличие, цену, законность или условия без подтверждённого источника.
8. Не отправлять сообщения реальным людям во время тестов.
9. Не запускать несколько polling-процессов с одним Telegram-токеном.
10. Перед production migration или maintenance создавать SQLite backup и
    проверять integrity и сохранность данных.
11. `.env`, токены, базы, backup, логи и персональные документы не добавлять в Git.
12. Передавать внешней AI-модели только обезличенный текст; прямые контакты и
    чувствительные идентификаторы обрабатываются локально.
13. Клиентские предложения отправляются только после явного решения владельца,
    независимо от исторических настроек `auto_handoff_enabled`.
14. Ручные лиды хранятся не дольше 30 дней и могут быть удалены владельцем сразу;
    очистка и обезличивание прежних записей выполняются при запуске основного бота.
15. Повторять внешний запрос можно только при временной сетевой ошибке, HTTP 429
    или 5xx, с ограниченным числом попыток и экспоненциальной задержкой.
16. Журналы не должны содержать исходные сообщения, контакты, ссылки, токены,
    ответы внешнего провайдера или полный текст исключения.
17. Новое партнёрское приглашение действует ограниченное время; по умолчанию
    24 часа, после успешного использования токен уничтожается.
18. Штатный backup создаётся командой
    `.\.venv\Scripts\python.exe -m scripts.backup_database`; существующий файл
    не перезаписывается, источник и копия проходят `integrity_check`.

## 7. Управление запуском

Владелец запускает основной бот вручную и должен понимать, работает ли процесс.

- Основной бот:
  `.\.venv\Scripts\python.exe bot.py`

Telegram-токену соответствует только один активный polling-процесс.
Ошибка `Conflict: terminated by other getUpdates request` означает, что бот с
тем же токеном уже запущен в другом процессе.

Scout-боты запускать не нужно. Их usernames не удаляются в BotFather, чтобы
сохранить бренд; старые токены необходимо отозвать вручную.

## 8. Следующие технические шаги

1. Провести live smoke на одном пересланном клиентском сообщении без отправки
   реальным клиентам или партнёрам.
2. Добавить выбор подходящих партнёров для структурированной заявки.
3. Сохранить обязательное подтверждение владельца перед отправкой.
4. После первых реальных заявок фиксировать скорость ответа, полезность ответа,
   конверсию и причины отказа партнёров.
5. Расширять категории только после появления реального запроса или доказанного
   узкого места.

## 9. Правила работы агента

Перед изменениями агент должен:

1. Прочитать этот файл полностью.
2. Проверить `git status`, текущую ветку и последние коммиты.
3. Сохранить пользовательские изменения и не трогать несвязанные файлы.
4. Использовать отдельную ветку для нового функционального блока.
5. Добавить targeted tests и запустить полный suite.
6. Проверить Python compilation, `git diff --check` и отсутствие секретов.
7. Не выполнять production migration, polling, live Telegram и outreach без
   явного разрешения владельца.
8. В отчёте объяснять результат простым языком, отдельно указывая, что было
   изменено, проверено, запущено и не запускалось.

## 10. Внешняя документация

Главная дорожная карта и партнёрские документы находятся в Google Drive.
Личный документ `ДНЕВНИК` редактирует только владелец. Этот файл хранит именно
технический и продуктовый контекст репозитория и не заменяет личный дневник.
